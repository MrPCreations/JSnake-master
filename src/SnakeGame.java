public class SnakeGame {
    public static void main(String[] args) {
        //Shortcut instead of writing GameFrame frame = new GameFrame.
        // I can use it because I plan to have just one object frame, so no instance field is required.
        new GameFrame();
    }
}
